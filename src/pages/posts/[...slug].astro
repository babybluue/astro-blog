---
import ContentRender from '@components/content-render.astro'
import NavHead from '@components/nav-header.astro'
import type { PostModel } from '@interfaces/post-model'
import BaseLayout from '@layouts/base-layout.astro'
import { getAllPosts } from '@utils/all-posts'

interface Props {
  post: PostModel
  prev: PostModel | undefined
  next: PostModel | undefined
}

export async function getStaticPaths() {
  const posts = await getAllPosts()
  return posts.map((post, index) => ({
    params: { slug: post.data.abbrlink },
    props: { post, prev: posts[index - 1], next: posts[index + 1] },
  }))
}

const { post, prev, next } = Astro.props
---

<BaseLayout title={post.data.title} description={post.data.descriptions ?? ''}>
  <body>
    <NavHead />
    <main class="flex flex-col justify-between">
      <ContentRender post={post} from="post" />

      <div class="mt-5 flex flex-wrap justify-between gap-3 py-2">
        {
          prev ? (
            <a
              class="dark:border-primary-gray w-[45%] truncate rounded-lg border px-5 py-2 text-left"
              href={prev.data.abbrlink}
            >
              {prev.data.title}
            </a>
          ) : (
            <span />
          )
        }
        {
          next ? (
            <a
              class="dark:border-primary-gray w-[45%] justify-items-end truncate rounded-lg border px-5 py-2 text-right"
              href={next.data.abbrlink}
            >
              {next.data.title}
            </a>
          ) : (
            <span />
          )
        }
      </div>
    </main>
  </body>
</BaseLayout>
